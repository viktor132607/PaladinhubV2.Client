services:
  - type: web
    name: paladinhubv2-client
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./out

    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://paladinhubv2-server.onrender.com

    routes:
      - type: rewrite
        source: /*
        destination: /index.html