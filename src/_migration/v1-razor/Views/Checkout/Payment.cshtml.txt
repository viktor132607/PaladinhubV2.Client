@model PaladinHub.Models.Checkout.PaymentVM
@{
    ViewData["Title"] = "Choose payment method";
}
<div id="payment-page" class="p-0">
    <style>
        #payment-page {
            --ph-bg: #0f1115;
            --ph-surface: #1b1f24;
            --ph-border: #2a3139;
            --ph-text: #e6e8eb;
            --ph-muted: #b1bac4;
            --ph-accent: #8ab4ff;
        }

            #payment-page, #payment-page * {
                color: var(--ph-text);
            }

                #payment-page .card {
                    background: var(--ph-surface);
                    border: 1px solid var(--ph-border);
                }

                #payment-page h2 {
                    color: var(--ph-accent);
                }

                #payment-page .form-check-label {
                    color: var(--ph-text);
                }

                #payment-page .form-check-input {
                    background-color: transparent;
                    border-color: var(--ph-muted);
                    accent-color: var(--ph-accent);
                }

                    #payment-page .form-check-input:focus {
                        box-shadow: 0 0 0 .2rem rgba(138, 180, 255, .25);
                        border-color: var(--ph-accent);
                    }

                #payment-page .btn-primary {
                    border-color: transparent;
                }
    </style>

    <div class="card p-4">
        <h2 class="mb-3">Choose payment method</h2>

        <form asp-action="Payment" asp-controller="Checkout" method="post">
            @Html.AntiForgeryToken()

            <div class="form-check mb-2">
                <input class="form-check-input" type="radio" asp-for="Method" value="Card" id="pmCard" />
                <label class="form-check-label" for="pmCard">Card</label>
            </div>

            <div class="form-check mb-2">
                <input class="form-check-input" type="radio" asp-for="Method" value="CashOnDelivery" id="pmCod" />
                <label class="form-check-label" for="pmCod">Cash on delivery</label>
            </div>

            <div class="form-check mb-4">
                <input class="form-check-input" type="radio" asp-for="Method" value="Balance" id="pmWallet" />
                <label class="form-check-label" for="pmWallet">Wallet Balance</label>
            </div>

            <button type="submit" class="btn btn-primary btn-lg w-100">Continue</button>
        </form>
    </div>
</div>
